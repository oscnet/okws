# 配置文件示例
config:
  redis_url: "redis://localhost"
  maxrow: 1000

servers:
  - name: test
    apiKey: ""
    secret: ""
    password: ""

subscribes:
  - server: test
    channels:
      - "spot/ticker:ETH-USDT"